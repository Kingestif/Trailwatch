export type TrendingType = {
    id: string
    title: string,
    name: string,
    poster_path: string,
    vote_average: string,
    release_date: string,
    first_air_date: string
}

export type MoviesType = {
    id: string
    title: string,
    poster_path: string,
    vote_average: string,
    release_date: string,
}